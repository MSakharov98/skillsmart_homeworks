'''
Задание 3

Исправление имен классвов и методов согласно полученным рекомендациям

Имена классов:

1. Было:`DataProcessor`
   Стало:`DataHandler` или `DataProcessor` (если обработка данных действительно единственная задача класса)

2. Было:`InfoManager`
   Стало:`InformationHandler` или `InformationManager`

3. Было:`UserValidator`
   Стало:`UserVerifier` или `UserValidationHandler`

4. Было:`ControllerManager`
   Стало:`ControlHandler` или `ControlManager`

5. Было:`Processor`
   Стало:`ProcessingUnit` или `DataProcessor`

Имена методов и объектов:

1. Было:`fetchData()`
   Стало:`retrieveData()` или `getData()`

2. Было:`calculateValues()`
   Стало:`computeValues()` или `calculate()`

3. Было:`handleError()`
   Стало:`processError()` или `handleException()`

4. Было:`userManager`
   Стало:`userHandler` или `userProcessor`

5. Было:`connectToDatabase()`
   Стало:`establishDatabaseConnection()` или `connectDatabase()`

6. Было:`validateInput()`
   Стало:`verifyInput()` или `inputValidation()`

7. Было:`ProtocolController`
   Стало:`CommunicationHandler` или `ProtocolHandler`

'''